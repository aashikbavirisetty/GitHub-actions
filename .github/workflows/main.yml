name: Generate Podcast Feed
on: [push]
jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Run feed generator
        uses: aashikbavirisetty/podcast---test@main
        
